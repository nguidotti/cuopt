# Configuration File for CodeCov
comment: false
coverage:
  status:
    project: off  # yamllint disable-line rule:truthy
    patch:
      default:
        target: auto
        threshold: 5%
        informational: true
github_checks:
  annotations: true
